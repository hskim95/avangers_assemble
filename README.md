# How to Use?
The main module is HackAssembler.py.

Open Terminal and type,

```python HackAssembler.py blahblah.asm```

By running the command line above, the assembler will generate byte code(blahblah.hack) from assembly code.
